# al-folio 个人网站搭建使用指南

本文档详细介绍如何使用 al-folio 模板搭建个人学术网站，包括需要修改的配置和具体使用步骤。

---

## 📋 目录

1. [必须修改的配置](#必须修改的配置)
2. [可选修改的配置](#可选修改的配置)
3. [本地开发环境搭建](#本地开发环境搭建)
4. [部署到 GitHub Pages](#部署到-github-pages)
5. [日常使用和维护](#日常使用和维护)
6. [常见问题](#常见问题)

---

## 🎯 必须修改的配置

### 1. 基本信息配置 (`_config.yml`)

**文件位置**: `_config.yml`

这是整个网站的核心配置文件，**必须**修改以下内容：

#### 1.1 个人信息（第5-9行）
```yaml
title: blank  # 网站标题，如果留空则使用姓名
first_name: 你的名字
middle_name: 你的中间名（可选）
last_name: 你的姓氏
```

#### 1.2 网站描述（第11-16行）
```yaml
description: >  # 网站描述，用于SEO
  你的网站描述，比如：一名专注于XX领域的研究者，来自XX大学
  
footer_text: >  # 页脚文本（可选）
  可以自定义页脚内容
```

#### 1.3 网站URL（第21-22行）**非常重要！**
```yaml
url: https://你的GitHub用户名.github.io  # 如果是用户名.github.io仓库，这样写
# 或者
url: https://你的域名.com  # 如果使用自定义域名

baseurl: ""  # 如果仓库名是 用户名.github.io，留空
# 或者
baseurl: /仓库名  # 如果仓库名不是 用户名.github.io，填写仓库名
```

**示例**:
- 如果你的GitHub用户名是 `robben-ge`，仓库名是 `robben-ge.github.io`
  ```yaml
  url: https://robben-ge.github.io
  baseurl: ""
  ```
- 如果仓库名是 `my-website`
  ```yaml
  url: https://robben-ge.github.io
  baseurl: /my-website
  ```

#### 1.4 语言设置（第18行）
```yaml
lang: zh  # 如果网站是中文，改为 zh（或 en, fr, cn, ru 等）
```

#### 1.5 博客名称（第89-90行）
```yaml
blog_name: 你的博客名称
blog_description: 你的博客描述
```

#### 1.6 Google Scholar 配置（第267-268行）
```yaml
scholar:
  last_name: [你的姓氏]
  first_name: [你的名字, 缩写]
```

**示例**:
```yaml
scholar:
  last_name: [Zhang]
  first_name: [San, S.]
```

---

### 2. 关于页面 (`_pages/about.md`)

**文件位置**: `_pages/about.md`

这是网站首页，需要修改：

#### 2.1 页面标题和副标题（第2-5行）
```yaml
---
layout: about
title: about
permalink: /  # 这是首页
subtitle: <a href='你的机构链接'>机构名称</a>. 地址. 联系方式. 座右铭等
```

#### 2.2 个人照片（第7-14行）
```yaml
profile:
  align: right  # 图片位置：left 或 right
  image: prof_pic.jpg  # 照片文件名
  image_circular: false  # true=圆形照片，false=方形
  more_info: >
    <p>办公室号</p>
    <p>地址</p>
    <p>城市, 国家 邮编</p>
```

**然后需要**：
1. 将你的照片命名为 `prof_pic.jpg`
2. 放到 `assets/img/` 目录下

#### 2.3 个人简介（第30行开始）
在文件底部，用 Markdown 格式写你的个人简介。

---

### 3. 社交链接 (`_data/socials.yml`)

**文件位置**: `_data/socials.yml`

取消注释并填写你使用的社交平台：

```yaml
email: your.email@example.com  # 必须修改

# 常用平台，取消注释并填写
github_username: 你的GitHub用户名
linkedin_username: 你的LinkedIn用户名
scholar_userid: 你的Google Scholar ID  # 从Google Scholar个人资料URL中获取
twitter_username: 你的Twitter用户名
orcid_id: 你的ORCID ID  # 学术研究者常用

# CV PDF
cv_pdf: /assets/pdf/你的简历.pdf  # 需要将PDF放到 assets/pdf/ 目录

# 其他平台根据需要启用...
```

**如何获取 Google Scholar ID**:
1. 访问 https://scholar.google.com
2. 登录你的账户
3. 访问你的个人资料页面
4. URL 类似：`https://scholar.google.com/citations?user=qc6CJjYAAAAJ`
5. `qc6CJjYAAAAJ` 就是你的 `scholar_userid`

---

### 4. 出版物 (`_bibliography/papers.bib`)

**文件位置**: `_bibliography/papers.bib`

使用 BibTeX 格式添加你的论文：

```bibtex
@article{einstein1935can,
  title={Can quantum-mechanical description of physical reality be considered complete?},
  author={Einstein, Albert and Podolsky, Boris and Rosen, Nathan},
  journal={Physical review},
  volume={47},
  number={10},
  pages={777},
  year={1935},
  publisher={APS},
  selected={true},  # 标记为精选论文，会在首页显示
  pdf={example_pdf.pdf}  # PDF文件名（放在 assets/pdf/ 目录）
}
```

**重要字段说明**:
- `selected={true}` - 在首页显示
- `pdf={文件名.pdf}` - 论文PDF（放在 `assets/pdf/`）
- `code={仓库链接}` - 代码仓库
- `video={视频链接}` - 视频链接
- `website={网站链接}` - 项目网站

你可以从 Google Scholar、DBLP 等网站导出 BibTeX 格式。

---

### 5. 简历/CV (`_data/cv.yml`)

**文件位置**: `_data/cv.yml`

修改为你的真实信息：

```yaml
- title: General Information
  type: map
  contents:
    - name: Full Name
      value: 你的全名
    - name: Date of Birth
      value: 出生日期
    - name: Languages
      value: 语言列表

- title: Education
  type: time_table
  contents:
    - title: 学位
      institution: 学校名称
      year: 年份
      description:
        - 描述1
        - 描述2

# 继续添加其他部分...
```

---

### 6. 项目展示 (`_projects/`)

**文件位置**: `_projects/1_project.md` 等文件

每个项目一个文件，修改示例项目或创建新的：

```markdown
---
layout: page
title: 项目名称
description: 项目描述
img: /assets/img/项目图片.jpg  # 放在 assets/img/ 目录
importance: 1  # 重要性，数字越大越靠前
category: work  # 分类：work, fun, other
---

项目详细介绍...
```

---

## 🔧 可选修改的配置

### 1. 新闻公告 (`_news/`)

在 `_news/` 目录创建 `.md` 文件来添加新闻：

```markdown
---
date: 2024-01-01
author: Your Name
categories: news
---

这是一条新闻内容...
```

### 2. 网站图标 (`_config.yml` 第19行)

```yaml
icon: 🚀  # 修改emoji或提供图片路径
```

### 3. 主题颜色 (`_sass/_themes.scss`)

修改主题主色调：
```scss
--global-theme-color: #your-color;
```

### 4. 博客文章 (`_posts/`)

- 删除示例文章或修改为你的文章
- 新文章命名格式：`YYYY-MM-DD-文章标题.md`

### 5. 分析工具

如果需要网站访问统计，在 `_config.yml` 中配置：

```yaml
google_analytics: G-XXXXXXXXXX  # Google Analytics ID
```

---

## 💻 本地开发环境搭建

### 前置要求

1. **Ruby** (版本 2.5 或更高)
   - Windows: 下载 RubyInstaller https://rubyinstaller.org/
   - Mac: `brew install ruby`
   - Linux: `sudo apt install ruby-full`

2. **RubyGems** (通常随 Ruby 一起安装)

3. **Bundler**
   ```bash
   gem install bundler
   ```

4. **Node.js** (可选，用于前端工具)
   - 下载：https://nodejs.org/

5. **Python 3** (用于学术引用工具)
   - 下载：https://www.python.org/

### 安装步骤

1. **进入项目目录**
   ```bash
   cd robben-ge.github.io
   ```

2. **安装 Ruby 依赖**
   ```bash
   bundle install
   ```
   如果遇到网络问题，可以使用国内镜像：
   ```bash
   bundle config mirror.https://rubygems.org https://gems.ruby-china.com
   bundle install
   ```

3. **安装 Python 依赖** (可选)
   ```bash
   pip install -r requirements.txt
   ```

4. **安装 Node.js 依赖** (可选)
   ```bash
   npm install
   ```

5. **启动本地服务器**
   ```bash
   bundle exec jekyll serve
   ```
   或者简写：
   ```bash
   bundle exec jekyll s
   ```

6. **访问网站**
   打开浏览器访问：http://localhost:4000

   **注意**：如果使用了 `baseurl`，访问地址是：http://localhost:4000/你的baseurl

### 常用命令

```bash
# 启动本地服务器（带自动刷新）
bundle exec jekyll serve

# 启动服务器（不监听外部连接，只能本机访问）
bundle exec jekyll serve --host 127.0.0.1

# 构建静态网站（不启动服务器）
bundle exec jekyll build

# 构建并查看详细输出
bundle exec jekyll build --verbose
```

---

## 🚀 部署到 GitHub Pages

### 方式一：GitHub Actions 自动部署（推荐）

这是最简单的方式，每次推送代码到 GitHub 会自动构建和部署。

1. **启用 GitHub Pages**
   - 打开仓库 Settings → Pages
   - Source 选择 "GitHub Actions"

2. **推送代码到 GitHub**
   ```bash
   git add .
   git commit -m "Initial commit"
   git push origin main  # 或 master，根据你的默认分支
   ```

3. **等待部署完成**
   - 在仓库的 Actions 标签页查看部署状态
   - 部署完成后，访问 `https://你的用户名.github.io`

### 方式二：手动构建并推送

1. **构建静态网站**
   ```bash
   bundle exec jekyll build
   ```

2. **推送 `_site` 目录**
   - 将构建后的 `_site` 目录内容推送到 `gh-pages` 分支

### 自定义域名

如果使用自定义域名：

1. **在 GitHub Pages 设置中添加域名**
   - Settings → Pages → Custom domain

2. **在项目根目录创建 `CNAME` 文件**
   ```
   你的域名.com
   ```

3. **更新 `_config.yml`**
   ```yaml
   url: https://你的域名.com
   baseurl: ""
   ```

---

## 📝 日常使用和维护

### 添加新博客文章

1. 在 `_posts/` 目录创建新文件
2. 文件名格式：`YYYY-MM-DD-文章标题.md`
3. 文件头部添加 YAML front matter：

```markdown
---
layout: post
title: 文章标题
author: Your Name
date: 2024-01-01
categories: [category1, category2]
tags: [tag1, tag2]
---

文章内容...
```

### 添加新项目

在 `_projects/` 目录创建新的 `.md` 文件，参考示例格式。

### 添加新出版物

在 `_bibliography/papers.bib` 文件中添加 BibTeX 条目。

### 更新新闻公告

在 `_news/` 目录创建新的 `.md` 文件。

### 本地预览

每次修改后，本地运行：
```bash
bundle exec jekyll serve
```
访问 http://localhost:4000 查看效果。

---

## ❓ 常见问题

### Q1: `bundle install` 失败怎么办？

**A**: 可能是网络问题，尝试：
```bash
# 使用国内镜像
bundle config mirror.https://rubygems.org https://gems.ruby-china.com
bundle install
```

### Q2: Jekyll 启动报错 "cannot load such file"

**A**: 确保已安装所有依赖：
```bash
bundle install
```

### Q3: 本地能访问，但 GitHub Pages 显示 404

**A**: 检查：
1. `_config.yml` 中的 `url` 和 `baseurl` 是否正确
2. GitHub Pages 设置中 Source 是否正确
3. 查看 Actions 标签页是否有构建错误

### Q4: 如何修改网站样式？

**A**: 
- 修改主题颜色：编辑 `_sass/_themes.scss`
- 修改布局：编辑 `_layouts/` 目录下的文件
- 修改样式：编辑 `_sass/` 目录下的 SCSS 文件

### Q5: 如何添加数学公式？

**A**: 已默认启用 MathJax，在 Markdown 中直接使用 LaTeX 语法：
```markdown
行内公式：$E = mc^2$

块级公式：
$$
\int_{-\infty}^{\infty} e^{-x^2} dx = \sqrt{\pi}
$$
```

### Q6: 如何添加代码高亮？

**A**: 使用 Markdown 代码块：
````markdown
```python
def hello():
    print("Hello, World!")
```
````

### Q7: 图片显示不出来？

**A**: 确保：
1. 图片路径正确（相对于 `assets/img/`）
2. 图片文件名没有特殊字符和中文
3. 使用 `/assets/img/图片名.jpg` 格式引用

### Q8: 如何关闭某个功能？

**A**: 在 `_config.yml` 中查找相应配置，设置为 `false` 或注释掉。

---

## 📚 更多资源

- **al-folio 官方文档**: https://github.com/alshedivat/al-folio
- **Jekyll 官方文档**: https://jekyllrb.com/
- **Markdown 语法**: https://www.markdownguide.org/
- **BibTeX 格式**: https://www.bibtex.org/

---

## ✅ 检查清单

在部署前，确认已完成：

- [ ] 修改 `_config.yml` 中的个人信息和URL
- [ ] 更新 `_pages/about.md` 个人简介
- [ ] 配置 `_data/socials.yml` 社交链接
- [ ] 添加个人照片到 `assets/img/prof_pic.jpg`
- [ ] 更新或删除示例出版物
- [ ] 更新或删除示例项目
- [ ] 测试本地运行 `bundle exec jekyll serve`
- [ ] 推送到 GitHub 并检查部署状态

---

**祝你搭建顺利！** 🎉

如果有问题，可以：
- 查看 [FAQ.md](FAQ.md)
- 查看 [CUSTOMIZE.md](CUSTOMIZE.md) 了解更多自定义选项
- 访问 al-folio GitHub Issues 寻求帮助

